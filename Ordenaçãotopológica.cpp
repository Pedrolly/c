#include <iostream>
#include <list>
using namespace std;
void dfs(list<no>adj[], int nVertices)
{

}
int dfs(int g)
{
	for(int i = 0; i <= u[g]; i++)
	{
		color[g] = 'w';
		pi[g] = 0;
	}
	time[g] = 0;
	for(int i = 0; i <= v[g]; i++)
	{
		if(color[u] = 'w')
		{
			dfsvisit(u);
		}
	}
}
int dfsvisit(u)
{
	color[u] = 'g';
	time[g] = time[g] + 1;
	d[u] = time[g];
	*v = adj[u];
	if(color[v] = 'w')
	{
  pi[v]=u[v];
  
	}
}
int main()
{
	char color[100];
	int pi[100], d[100], f[100];
	int time[100];
	int u[100], *v[100];
	int adj[100];
	cin >> g;
	for(int h; h <= 5; h++)
	{
		u = h;
	}
	return 0;
}
