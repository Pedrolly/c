#include <iostream>
#include <iomanip>
#include <cmath>

using namespace std;

int calcmod(int num)
{
	int f, modulo;
	if(num >= 0)
	{
		modulo = num;
		return modulo;
	}
	else
	{
		num*(-1) = modulo;
		return modulo;
	}
}

int main()
{
	int num, modulo;
	cout << "Entre com um numero inteiro" << endl;
	cin >> num;

	cout << modulo << endl;
	return 0;
}
int multi(int multi)
{
	int multi, i;
	for(i = 0; i < 9; i++)
	{
		num *i = multi;
		return multi;
	}
}
